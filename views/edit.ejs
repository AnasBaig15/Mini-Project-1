<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
      <h5 class="mb-5">You can edit your post</h5>
      <form action="/update/<%= post._id%>" method="post">
        <textarea
          class="block p-323 outline-none w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md"
          name="content"
          placeholder="What in Your mind ?"
        ><%= post.content %></textarea>
        <input
          class="px-3 w-40 py-2 text-sm bg-yellow-500 text-black text-bold block rounded-md mt-2"
          type="submit"
          value="Update Post"
        />
      </form>
    </div>
  </body>
</html>
